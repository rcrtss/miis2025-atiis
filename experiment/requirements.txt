# Experiment runtime deps (all already present in the dev-atiis env).
# No provider SDK: OpenRouter is called directly over httpx.
httpx>=0.28
pydantic>=2.10
python-dotenv>=1.0
tenacity>=9.0
pandas>=2.2
numpy>=2.0
scipy>=1.15
matplotlib>=3.10
tqdm>=4.66

# Optional: only for the extra mixed-model cell in analysis/analysis.ipynb.
# The notebook's variance-decomposition (ICC-style) summary runs without it.
# statsmodels>=0.14
